# Remove this value after the first hosted wiki is bootstrapped.
BOOTSTRAP_OWNER_EMAIL=owner@example.com

# Enable only for a public demo deployment. Signed-in visitors receive an
# isolated, quota-limited editor vault and never join an existing owner vault.
PUBLIC_DEMO_AUTO_ONBOARD=false
